**Options**

Options for SQL format are the same as for Json (catima export):

![Output sql AND](assets/export/export-options-catima-sql-en.png) 


**Results** 

However, there are fewer exported files, and the overall information included in this export is less complete that the information included in the JSON export:

1.	Both the data **structure** ...
2.	... and the **data** itself are within the unique \*.sql file for all *item-types* (here: *mes-donnees-sb.sql*)
3.	(optionnal) the *files* and *images* are in folder **files**
4.  The **export metadata** (i.e. date and time of the export) is saved in file *meta.json*

![Output sql AND](assets/export/export-output-sql-small.png) 

![CSV Output AND](assets/export/export-output-sql.png) 